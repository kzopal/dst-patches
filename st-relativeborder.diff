diff -ruN -x '*.orig' -x '*.rej' ../src/st.h ./st.h
--- ../src/st.h	2026-06-19 13:37:35.505823446 -0700
+++ ./st.h	2026-06-19 15:25:49.501041086 -0700
@@ -52,6 +52,7 @@
 	SNAP_LINE = 2
 };
 
+int borderpx;
 typedef unsigned char uchar;
 typedef unsigned int uint;
 typedef unsigned long ulong;
diff -ruN -x '*.orig' -x '*.rej' ../src/x.c ./x.c
--- ../src/x.c	2026-06-19 14:34:02.727203628 -0700
+++ ./x.c	2026-06-19 15:25:49.501041086 -0700
@@ -1033,6 +1033,8 @@
 	win.cw = ceilf(dc.font.width * cwscale);
 	win.ch = ceilf(dc.font.height * chscale);
 
+	borderpx = ceilf(((float)borderperc / 100) * win.cw);
+
 	FcPatternDel(pattern, FC_SLANT);
 	FcPatternAddInteger(pattern, FC_SLANT, FC_SLANT_ITALIC);
 	if (xloadfont(&dc.ifont, pattern))
